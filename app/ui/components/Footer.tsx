export default function Footer() {
    return (
        <footer className="max-w-sm mt-24 text-slate-400 dark:text-slate-500 text-sm">
            <p>Coded in Visual Studio Code by myself. Built with Next.js, Tailwind CSS deployed as a Docker container on an VPS with Traefik as a reverse proxy.</p>
        </footer>
    );
}